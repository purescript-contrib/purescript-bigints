{
  "scripts": {
    "postinstall": "bower install",
    "build": "pulp test && rimraf docs && pulp docs"
  },
  "dependencies": {
    "big-integer": "^1.6.17"
  },
  "devDependencies": {
    "pulp": "^9.0.0",
    "rimraf": "^2.5.2"
  }
}
