{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build -- --censor-lib --strict",
    "test": "pulp test"
  },
  "dependencies": {
    "big-integer": "^1.6.27"
  },
  "devDependencies": {
    "bower": "^1.8.8",
    "pulp": "^15.0.0",
    "rimraf": "^2.6.1"
  }
}
