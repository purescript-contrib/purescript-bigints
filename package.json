{
  "scripts": {
    "postinstall": "bower install",
    "build": "pulp test && rimraf docs && pulp docs"
  },
  "dependencies": {
    "big-integer": "^1.6.9"
  },
  "devDependencies": {
    "pulp": "^6.2.0",
    "rimraf": "^2.4.1"
  }
}
