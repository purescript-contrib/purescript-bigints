{
  "scripts": {
    "postinstall": "bower install",
    "build": "pulp test && rimraf docs && pulp docs"
  },
  "dependencies": {
    "big-integer": "^1.6.27"
  },
  "devDependencies": {
    "bower": "^1.8.4",
    "pulp": "^13.0.0",
    "rimraf": "^2.5.2"
  }
}
